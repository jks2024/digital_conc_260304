package 자동차만들기;

public interface Audio {
    void audioOnOFF(boolean isOn); // public abstract
}

interface AutoDrive {
    void autoOnOff(boolean isOn);
}

interface AirCon {
    void airConOnOff(boolean isOn);
}